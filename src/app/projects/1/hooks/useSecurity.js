export const useSecurity = () => {
    const security = {
      src: "/images/remoteDesktop/Seguridad.png", // Ruta de la imagen
      alt: "Seguridad de la maquina remota", // Descripción de la imagen
    };
  
    return { security };
  };
  