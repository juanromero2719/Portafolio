export const useObserver = () => {
    const observer = {
      src: "/images/remoteDesktop/Observer.png", // Ruta de la imagen
      alt: "Observer de la maquina remota", // Descripción de la imagen
    };
  
    return { observer };
  };
  